package com.user.bemp.view.viewholder

import android.view.View
import androidx.recyclerview.widget.RecyclerView.ViewHolder
import com.user.bemp.databinding.ListBeritaBinding

class BeritaViewHolder(itemView: View) : ViewHolder(itemView) {
    val binding = ListBeritaBinding.bind(itemView.rootView)
}